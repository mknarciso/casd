<p id="notice"><%= notice %></p>
<link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet"/>
<link href="https://cdnjs.cloudflare.com/ajax/libs/datatables/1.10.12/css/dataTables.bootstrap.min.css" rel="stylesheet"/>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/datatables/1.10.12/js/jquery.dataTables.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/datatables/1.10.12/js/dataTables.bootstrap.min.js"></script>
<script>
  $(document).ready(function() {
  $('#candidateFormsTable').DataTable();
});
</script>

<h1>Listando Formulários dos Candidatos | <%= link_to "Selecionar Colunas", candidate_forms_selection_path %></h1>

<table id="candidateFormsTable" class="table table-striped table-bordered" cellspacing="0" width="100%">
  <thead>
    <tr>
      <% @columns.each do |s| %>
        <th><%= s.camelcase %></th>
      <% end %>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @candidate_forms.each do |candidate_form| %>
      <tr>
        <% @columns.each do |s| %>
          <td><%= candidate_form[s.to_sym] %></td>
        <% end %>
        <td><%= link_to 'Show', candidate_form %></td>
        <td><%= link_to 'Edit', edit_candidate_forms_path(candidate_form) %></td>
        <td><%= link_to 'Destroy', candidate_form, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Candidato', new_candidato_path %>
<%= link_to 'Importar Lista CSV', addfile_candidatos_path%>